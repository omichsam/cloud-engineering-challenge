services:
  node-app:
    build: .
    ports: ["3001:3000"]
    environment:
      MONGO_HOST: mongodb
      MONGO_PORT: 27017
      MONGO_DATABASE: attendance_db
    depends_on: [mongodb]
  mongodb:
    image: mongo:7
    volumes: [mongodb-data:/data/db]
volumes:
  mongodb-data:
