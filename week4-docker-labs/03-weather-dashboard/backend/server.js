const express=require('express');const app=express();app.use(express.json());const searches=[];app.get('/api/weather',(q,r)=>r.json({city:q.query.city||'Nairobi',temperature:24,condition:'Sunny',humidity:55,windSpeed:12}));app.get('/api/searches',(_,r)=>r.json(searches));app.post('/api/searches',(q,r)=>{searches.push(q.body.city);r.status(201).json({city:q.body.city})});app.listen(5000,'0.0.0.0');
