# EC2 deployment
Launch Ubuntu 22.04, configure key pair and security group, install Docker, create `task-tracker-network`, run MySQL with `task-tracker-db-data`, and run the provided image on port 80. Follow `ec2/README.md` and the root README. Expected results must be verified by the student.
